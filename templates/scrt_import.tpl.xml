<?xml version="1.0" encoding="UTF-8"?>
<VanDyke version="3.0">

	<!-- Sessions -->
	<key name="Sessions">
	
		<key name="666 LAB">
			<key name="test2">

				{%- for session in sessions %}
				<key name="{{ session.key }}">
					<string name="Hostname">{{ session.hostname }}</string>
                    <dword name="[SSH2] Port">{{ session.port }}</dword>
					{%- if session.username is defined %}
                    <string name="Username">{{ session.username }}</string>
					{%- endif %}
					{%- if session.credential is defined %}
                    <string name="Credential Title">{{ session.credential }}</string>
					{%- endif %}
					<string name="SSH2 Authentications V2">keyboard-interactive</string>
					<string name="Cipher List">aes256-ctr,aes256-cbc</string>
					{%- if session.keyword is defined %}
					<string name="Keyword Set">{{ session.keyword }}</string>
                    <dword name="Highlight Bold">0</dword>
					<dword name="Highlight Color">1</dword>
					<dword name="Highlight Reverse Video">0</dword>
					{%- endif %}
				</key>
				{%- endfor %}
			</key>
		</key>
	</key>
	
	<!-- Credentials -->
	<key name="Credentials">
		{%- for credential in credentials %}
		<key name="{{ credential.key }}">
			{%- if credential.username is defined %}
			<string name="Username">{{ credential.username }}</string>
			{%- endif %}
			<string name="Key Exchange Algorithms">ecdh-sha2-nistp256,ecdh-sha2-nistp384,ecdh-sha2-nistp521,diffie-hellman-group18-sha512,diffie-hellman-group16-sha512</string>
			<dword name="Use Global Host Key Algorithms">1</dword>
			{#- <string name="Host Key Algorithms">ssh-dss,ssh-rsa,ecdsa-sha2-nistp256,ecdsa-sha2-nistp384,ecdsa-sha2-nistp521,null,x509v3-sign-rsa,x509v3-sign-dss</string> #}
			<string name="Cipher List">aes256-ctr,aes256-cbc</string>
			<string name="MAC List">hmac-sha1,hmac-sha1-96,hmac-sha2-512,hmac-sha2-256</string>
			<string name="SSH2 Authentications V2">password,keyboard-interactive</string>
			{#- <string name="SSH2 Authentications V2">keyboard-interactive</string> #}
		</key>
		{%- endfor %}
	</key>

</VanDyke>    